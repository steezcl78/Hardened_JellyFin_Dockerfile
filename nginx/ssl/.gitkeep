# This file ensures the ssl directory is tracked by git.
# The actual certificate files (*.pem) are generated locally and gitignored.
#
# To generate certificates, run:
#   cd nginx/ssl
#   openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout key.pem -out cert.pem -config openssl.cnf
